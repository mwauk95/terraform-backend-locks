# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.backup

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data,
# unless the user explicitly commits them.
*.tfvars

# Ignore override files as they are usually used for local customizations
override.tf
override.tf.json
*_override.tf

# Include tfplan files to ignore the plan output
*.tfplan

# Ignore state files from local backends
terraform.tfstate
terraform.tfstate.backup
